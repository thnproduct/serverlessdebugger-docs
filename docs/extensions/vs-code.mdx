---
sidebar_position: 2
slug: /extensions/vs-code
---

# VS Code
![Marketplace Download](https://vsmarketplacebadge.apphb.com/downloads-short/thundra.thundra-debugger.svg)
![Version](https://vsmarketplacebadge.apphb.com/version-short/thundra.thundra-debugger.svg)
![Rating](https://vsmarketplacebadge.apphb.com/rating-short/thundra.thundra-debugger.svg)

Serverless Debugger's VS Code extension allows you to debug your AWS Lambda functions written in [NodeJS](/configuration/nodejs) and [Python](/configuration/python). With a small configuration, debugging is as simple as a click of a button.

## Introduction 
Start online debug sessions while the AWS Lambda functions run on AWS Cloud. Thundra sets up an 
easy and secure connection between your AWS Lambda functions and VSCode. Debug your serverless 
applications natively with their own permissions. Stop mocking resources and simulating the environments! 
Put breakpoints and pause the execution of the [NodeJS](/configuration/nodejs) and [Python](/configuration/python) functions to see the stack trace.

## Installation
In order to use Serverless Debugger's VS Code extension, you need to **complete** all installation steps. Please follow the instructions below. 

import {Step} from '@site/src/components/Step';
import {Container} from '@site/src/components/Container';

<Container>
  <Step step="1" stepHeader="Install the extension from the marketplace">
    <a href="https://marketplace.visualstudio.com/items?itemName=thundra.thundra-debugger">See it on VS Code Marketplace</a>
  </Step>
  <Step step="2" stepHeader="Get your Debug Token">
    <a href="https://app.serverlessdebugger.com">
    Sign up for ServerlessDebugger</a> and get your <code>SLSDEBUGGER_AUTH_TOKEN</code> from <a href="https://app.serverlessdebugger.com/debug">this page</a>.
    <br/><br/>

:::info
If you want to try ServerlessDebugger you can get a free 7 days auth token from [here](https://app.serverlessdebugger.com/request-token)
:::


  </Step>
  <Step step="3" stepHeader="Edit VS Code and AWS Environment variables">
  <ul>
    <li>
      Open the Command Palette <code>(⇧⌘P) or (Ctrl + Shift + P)</code> and select <strong>ServerlessDebugger: Edit Configuration</strong> command.
      <br/><br/>
      <img src='/img/vscode-commands.png' alt="VS Code Status Bar" />
      <small>VS Code Command Panel, select ServerlessDebugger: Edit Configuration</small>
      <br/><br/>
    </li>
    <li>
      Paste the auth token to the <code>authToken</code> field.
      <br/><br/>
      <img src='/img/vscode-configfile.png' alt="VS Code Status Bar" />
      <small>VS Code Configuration file, paste your <code>authToken</code></small>
    </li>
  </ul>
  </Step>
  <Step step="4" stepHeader="Edit AWS Environment variables">
    Go to your <strong>AWS Console</strong> and set your auth token value with the <code>SLSDEBUGGER_AUTH_TOKEN</code> key as your environment variable.
    <br/><br/>
    <img src='/img/awsconsole-authtoken.png' alt="VS Code Status Bar" />
  </Step>
  <Step step="5" stepHeader="Instrument your function">
    After installing the extension, instrument your AWS Lambda. You can instrument your <a href="/configuration/nodejs">NodeJS</a> and <a href="/configuration/python">Python</a> applications to use VS Code Extension.
  </Step>
</Container> 

:::note

If you need any help, please [join our Discord channel](https://www.serverlessdebugger.com/discord-invitation) and get personal help from the contributors. 

:::

## How to use

After completing the installation steps, follow the guides below to start using Lambda Debugger.

<Container>
  <Step step="▶️" stepHeader="Start debugger">
    Click on the “Start ServerlessDebugger Debugger” button on the Status Bar. You can also execute the command ServerlessDebugger: Start Debugger to start the debugging session.
    <br/><br/>
    <img src='/img/vscode-status-bar.png' alt="VS Code Status Bar" />
  </Step>
  <Step step="🛑" stepHeader="Set a breakpoint">
    Set a debug point on your VS Code. You can set multiple debug points as well.
  </Step>
  <Step step="⚡️" stepHeader="Invoke your function">
    Now invoke your Lambda function to hit on the debug point. When there is a hit on your breakpoint the status bar below turns red.
    <br/><br/>
    <img src='/img/vscode-connected.png' alt="VS Code Status Bar" />
  </Step>
  <Step step="🔍" stepHeader="Happy debugging!">
    You can start debugging when the variables and the call stack are loaded.
    <br/><br/>
    <img src='/img/vscode-variables.png' alt="VS Code Status Bar" />
  </Step>
</Container>    

:::tip

Debug session ends when your AWS Lambda function times out. You can update the timeout of your function for longer debug sessions.

:::

## Commands

- ```ServerlessDebugger: Start Debugger``` to start the debugger with the current profile's settings
- ```ServerlessDebugger: Start Debugger (with a session name)``` to start the debugger with the given session name
- ```ServerlessDebugger: Change active profile``` to change the active profile to the one given in your config file
- ```ServerlessDebugger: Edit configuration``` to edit the JSON file containing your debugger configuration

You can access all of the above commands from the command palette ```(Cmd+Shift+P or Ctrl+Shift+P)```.

## Self-Hosted Broker

A self-hosted instance of the broker that provides the necessary functionality for online debugging can be installed 
on your own AWS account using our CloudFormation template or our CDK application. Please follow [this guide](/self-hosted-broker/CloudFormation) to learn more.

## Issues

Issues can be reported through the [Thundra Debugger Issue Repository](https://github.com/thundra-io/thundra-vscode-issues).