---
sidebar_position: 2
slug: /extensions/vs-code
---

# VS Code
![Marketplace Download](https://vsmarketplacebadge.apphb.com/downloads-short/thundra.thundra-debugger.svg)
![Version](https://vsmarketplacebadge.apphb.com/version-short/thundra.thundra-debugger.svg)
![Rating](https://vsmarketplacebadge.apphb.com/rating-short/thundra.thundra-debugger.svg)
![Community Channel](https://badgen.net/badge/Slack/Lambda%20Debugger?icon=slack)

Lambda Debugger's VS Code extension allows you to debug your AWS Lambda functions. It currently supports NodeJS and Python runtimes. With a small configuration, debugging is as simple as a click of a button.

## Introduction 
Start online debug sessions while the AWS Lambda functions run on AWS Cloud. Thundra sets up an easy and secure connection between your AWS Lambda functions and VSCode. Debug your serverless applications natively with their own permissions. Stop mocking resources and simulating the environments! Put breakpoints and pause the execution of the Node.js and Python functions to see the stack trace.

## Installation
In order to use AWS Lambda Debugger, you need to **complete** all installation steps. Please follow the instructions below. 

import {Step} from '@site/src/components/Step';
import {Container} from '@site/src/components/Container';
import {vscodeStatusBar} from '@site/static/img/vscode-status-bar.png';

<Container>
  <Step step="1" stepHeader="Install the extension from the marketplace">
    <a href="#">See it on VS Code Marketplace</a>
  </Step>
  <Step step="2" stepHeader="Get your Debug Token">
    <a href="/">Sign up for Thundra</a> and get your <code>THUNDRA_AGENT_LAMBDA_DEBUGGER_AUTH_TOKEN</code> from <a href="/">this page</a>.
  </Step>
  <Step step="3" stepHeader="Edit VS Code and AWS Environment variables">
  <ul>
    <li>
      Open the Command Palette <code>(⇧⌘P) or (Ctrl + Shift + P)</code> and select <strong>Thundra: Edit Configuration</strong> command to paste the auth token to the authToken field.
    </li>
    <li>
      Go to your <strong>AWS Console</strong> and set your auth token value with the <code>THUNDRA_AGENT_LAMBDA_DEBUGGER_AUTH_TOKEN</code> key as your environment variable.
    </li>
  </ul>
  </Step>
  <Step step="4" stepHeader="Instrument your function">
    After installing the extension, instrument your AWS Lambda. You can instrument your <a href="#">NodeJS</a> and <a href="#">Python</a> applications to use VS Code Extension.
  </Step>
</Container> 

:::note

If you need any help, please [join our Slack channel](#) and get personal help from the contributors. 

:::


## How to use

After completing the installation steps, follow the guides below to start using Lambda Debugger.

<Container>
  <Step step="▶️" stepHeader="Start debugger">
    Click on the “Start Thundra Debugger” button on the Status Bar. You can also execute the command Thundra: Start Debugger to start the debugging session.
    <br/><br/>
    <img src='/img/vscode-status-bar.png' alt="VS Code Status Bar" />
  </Step>
  <Step step="🛑" stepHeader="Set a breakpoint">
    Set a debug point on your VS Code. You can set multiple debug points as well.
  </Step>
  <Step step="⚡️" stepHeader="Invoke your function">
    Now invoke your Lambda function to hit on the debug point. When there is a hit on your breakpoint the status bar below turns red.
    <br/><br/>
    <img src='/img/vscode-connected.png' alt="VS Code Status Bar" />
  </Step>
  <Step step="🔍" stepHeader="Happy debugging!">
    You can start debugging when the variables and the call stack are loaded.
    <br/><br/>
    <img src='/img/vscode-variables.png' alt="VS Code Status Bar" />
  </Step>
</Container>    

:::tip

Debug session ends when your AWS Lambda function times out. You can update the timeout of your function for longer debug sessions.

:::

## Commands

- ```Thundra: Start Debugger``` to start the debugger with the current profile's settings
- ```Thundra: Start Debugger (with a session name)``` to start the debugger with the given session name
- ```Thundra: Change``` active profile to change the active profile to the one given in your config file
- ```Thundra: Edit``` configuration to edit the JSON file containing your debugger configuration

You can access all of the above commands from the command palette ```(Cmd+Shift+P or Ctrl+Shift+P)```.

## Self-Hosted Broker

A self-hosted instance of the broker that provides the necessary functionality for online debugging can be installed on your own AWS account using our CloudFormation template or our CDK application. Please follow [this guide](#) to learn more.

## Issues

Issues can be reported through the [Thundra Debugger Issue Repository](https://github.com/thundra-io/thundra-vscode-issues).